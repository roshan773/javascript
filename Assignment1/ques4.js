// Calculate and store the result of the following operations in variables:

// - Addition of `x` and `y`
// - Multiplication of `x` and `y`
// - Division of `x` by `y`
// - Subtraction of `x` from `y`

var x = 5, y = 3;

let a = x + y;
let b = x - y;
let c = x * y;
let d = x / y;

console.log("The addition of x and y is =>", a);
console.log("The substraction of x and y is =>", b);
console.log("The Multiplication of x and y =>", c);
console.log("The Division of x and y is =>", d);